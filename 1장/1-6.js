let friends = ["민준", "서윤", "지우", "지호", "준우"];
friends = friends.map(name => "김" + name);

// forEach & map
// 공통점 : 배열 값을 조작하여 원하는 결과값을 얻을 수 있다.

// forEach() : 배열 요소마다 한 번씩 주어진 함수를 실행한다.
// map()     : 배열 내의 모든 요소 각각에 주어진 함수(콜백)를 호출한 결과를 모아 새로운 배열을 반환한다.

